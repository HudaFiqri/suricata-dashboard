# IMPORTANT: This is a template file
# DO NOT commit actual secrets to git!
#
# Create secrets using one of these methods:
#
# Method 1: kubectl create secret
# kubectl create secret generic dashboard-secrets \
#   --from-literal=DB_PASSWORD='your-secure-password' \
#   --namespace=suricata-monitoring
#
# Method 2: sealed-secrets (recommended for GitOps)
# kubectl create secret generic dashboard-secrets --dry-run=client -o yaml \
#   --from-literal=DB_PASSWORD='your-password' | \
#   kubeseal -o yaml > sealed-secret.yaml
#
# Method 3: External Secrets Operator
# Use with HashiCorp Vault, AWS Secrets Manager, etc.
#
# Method 4: For testing only (NOT FOR PRODUCTION)
# Apply this file after changing the password

apiVersion: v1
kind: Secret
metadata:
  name: dashboard-secrets
  namespace: suricata-monitoring
  labels:
    app: suricata-dashboard
type: Opaque
stringData:
  # CHANGE THIS PASSWORD!
  DB_PASSWORD: "changeme"
